# 영상 편집 자동화 도구 — 기획서 (캡컷 연계 버전)

> 원본 기획서. 확정된 설계는 `DESIGN.md`를 보세요.

---

## 1. 목표

촬영 폴더 경로와 조건(날짜 등)만 주면 해당 영상들을 골라 하나로 합치고,
**무음/필러워드가 잘린 mp4 + 그에 맞는 srt 자막 파일**을 내놓는 로컬 도구.

이후 편집(B-roll, 효과음, 인트로, 강조 자막)은 **캡컷에서 직접** 진행한다.
따라서 이 도구는 노가다 구간(전사·컷·자막 타이핑)만 처리한다.

- 실행 환경: macOS / Windows 둘 다
- 영상 파일은 외부로 전송하지 않음 (전사는 로컬 Whisper)
- 코딩 지식 없이 대화로 실행 가능해야 함

---

## 2. 입출력

**원본 파일은 옮기지 않는다.** 촬영 폴더 경로를 지정하면 도구가 알아서 골라온다.
원본은 읽기 전용으로만 접근하고, 절대 수정·이동·삭제하지 않는다.

```
work/            중간 산출물 (merged.mp4, transcript.json, cutlist.json)
output/
  final.mp4      병합 + 컷 적용된 영상 (자막 안 구움)
  final.srt      캡컷에 임포트할 자막
config.yaml      설정 파일
glossary.txt     고유명사 사전
```

실행 예시:
```bash
# 특정 날짜에 촬영된 파일만 골라서 하나로 합치기
python run.py --dir "D:/촬영/2026" --date 2026-02-17 --merge

# 파일명 패턴으로 고르기
python run.py --dir "D:/촬영/2026" --filter "160217*" --merge

# 단일 파일
python run.py "D:/촬영/2026/160217_001.mp4"
```

**중요: 자막은 영상에 굽지 않는다(하드코딩 금지).**
캡컷에서 자막을 수정·이동·스타일 변경해야 하므로 반드시 srt로 분리 출력한다.

---

## 3. 파이프라인

### Step 0. 파일 수집 + 정렬 (신규)
- `--dir`로 받은 폴더를 스캔 (하위 폴더 포함 여부는 config로)
- 필터 조건:
  - `--date` : **파일 메타데이터의 촬영 시각** 기준 (파일명 아님)
  - `--filter` : 파일명 glob 패턴
  - 확장자: mp4, mov, mts, avi (config에서 확장 가능)
  - **사진·기타 파일(jpg, heic, raw, png 등)은 확장자 단계에서 자동 제외**
  - **최소 길이 미만 클립 자동 제외 (기본 3초, config에서 조절)**
    - 아이폰·카메라의 Live Photo, 실수로 찍힌 짧은 클립이 .mov로 섞여 들어오는 것 방지
    - 제외된 파일은 목록에 "제외됨: 길이 1.2초"처럼 표시해서 사용자가 알 수 있게
- **정렬은 메타데이터의 촬영 시각(creation_time) 오름차순**
  - 파일명순 금지 — 카메라 파일명은 순환한다 (DSC_9999 → DSC_0001)
  - ffprobe로 `format_tags=creation_time` 추출
  - 메타데이터에 촬영 시각이 없으면 파일 수정일(mtime)로 폴백하고, 그 사실을 사용자에게 경고
- 원본 경로 목록을 `work/sources.json`에 저장

### Step 1. 소스 검사
- 수집된 각 파일에 대해 ffprobe로 길이 / 해상도 / fps / 코덱 / 오디오 트랙 확인
- 오디오 트랙 없는 파일은 목록에서 제외하고 알림
- **해상도·fps·코덱이 파일마다 다르면 경고 표시** (그냥 붙이면 깨짐)
  - 첫 클립 기준으로 통일할지, 중단할지 사용자에게 물어봄

### Step 1.5. 검수 게이트 0 — 파일 목록 확인 ⚠️ 필수
병합 전에 **반드시 멈추고** 아래를 보여준 뒤 승인받기:
```
D:/촬영/2026 에서 8개 파일 찾음 (촬영일 2026-02-17)
정렬 기준: 촬영 시각(메타데이터)

 1. 160217_001.mp4  14:02:11  02:14  1920x1080 30fps h264
 2. 160217_002.mp4  14:09:33  05:31  1920x1080 30fps h264
 ...
 8. 160217_008.mp4  16:44:02  01:02  3840x2160 30fps h264  ⚠️ 해상도 다름

제외됨 (2개):
 - 160217_003.mov  00:01  ← 최소 길이 3초 미만
 - 160217_006.mp4  02:10  ← 오디오 트랙 없음

총 24분 18초 → work/merged.mp4
이 순서로 진행할까요? (y / 파일 제외 / 순서 변경 / 제외 취소 / 중단)
```
엉뚱한 파일이 딸려 들어오거나 순서가 뒤집히는 게 가장 흔한 사고이므로 생략 금지.

### Step 1.8. 병합
- 승인된 순서대로 `work/merged.mp4` 생성
- **반드시 병합을 먼저 하고 그 다음 전사·컷을 진행** (순서가 바뀌면 타임코드가 꼬임)
- 스펙이 동일하면 스트림 카피(concat demuxer)로 빠르게, 다르면 재인코딩
- 파일이 1개뿐이면 병합 생략하고 그대로 사용

### Step 2. 전사 (STT)
- 로컬 Whisper 사용, 한국어 설정
- **단어 단위 타임스탬프** 필수 (`word_timestamps=True`)
- 결과를 `work/transcript.json`에 저장
- 이미 transcript.json이 있으면 재실행하지 않고 재사용 (캐시)

### Step 3. 컷 후보 산출

**핵심 원칙: 무음 = 삭제 대상이 아니다.**
브이로그의 풍경/B-roll 구간은 원래 말이 없다. 이걸 자르면 안 된다.

컷 후보로 삼는 조건 — 아래를 **모두** 만족할 때만:
- 0.5초 이상 침묵
- **앞뒤가 모두 발화 구간인 무음** (말하다가 생긴 빈 구간)
- 길이가 `keep_silent_over` 미만 (기본 10초)

즉 **연속 무음이 10초를 넘으면 B-roll로 간주하고 컷 대상에서 자동 제외**한다.
config.yaml의 `keep_silent_over`로 조절.

기타:
- 컷 지점 앞뒤로 0.1초 여유(패딩)를 남겨서 말이 뭉개지지 않게
- 영상 맨 앞/맨 뒤의 무음은 인트로/아웃트로일 수 있으므로 컷 후보에서 제외
- **필러워드: "어", "음" 만 제거** (보수적 설정)
  - "그러니까", "약간", "이제" 등은 제거하지 않음
  - 단독으로 발화된 것만 제거 (단어 안에 포함된 경우 제외)
- `--protect 04:20-05:10` 옵션으로 특정 구간을 강제 보호 (여러 개 지정 가능)
- 결과를 `work/cutlist.json`에 저장 (각 항목에 `reason` 필드: silence / filler)

### Step 4. 검수 게이트 1 — 컷 확인 (썸네일 미리보기) ⚠️ 필수

**텍스트 목록만으로는 풍경 컷인지 판단할 수 없다.** 반드시 화면을 보여줄 것.

방식 (리소스 최소):
- 컷 후보 구간마다 **프레임 1장만 추출** (구간 중간 지점)
- 크기는 320px 폭, jpg — 클립을 잘라내지 않으므로 매우 빠름
- 애매한 구간(2초 이상)만 **앞·중간·뒤 3장** 추출
- `work/preview.html` 하나를 생성 → 브라우저로 열면 확인

preview.html 구성:
```
컷 후보 48개 · 원본 12:34 → 예상 09:12

[✓] #1  02:11~02:12  1.2초  무음     [썸네일]
[✓] #2  03:21~03:26  5.2초  무음     [썸네일 3장]
[ ] #3  05:40~05:58  18초   무음     [썸네일 3장]  ← 자동 제외됨(B-roll 추정)
[✓] #4  06:02~06:03  0.7초  필러 "어" [썸네일]

체크 해제한 구간은 살립니다.
[선택 저장]  → work/cutlist.json 갱신
```

- 체크박스만 있는 순수 HTML + JS, 서버 불필요
- 저장하면 선택 결과가 cutlist.json에 반영됨
- 자동 제외된 긴 무음도 목록에 **회색으로 표시**해서, 원하면 다시 체크해 자를 수 있게
- 저장 후 터미널에서 엔터 → 다음 단계 진행

썸네일은 `work/thumbs/`에 저장하고, 재실행 시 이미 있으면 재추출하지 않는다.

### Step 5. 자막 생성 + 타임코드 재계산 ⚠️ 핵심
- transcript 기반으로 자막 생성
- **🚨 가장 중요: srt 타임코드는 반드시 "컷 적용 후" 기준으로 재계산할 것**
  - 원본 타임코드를 그대로 쓰면 캡컷에서 자막이 전부 뒤로 밀린다
  - 각 자막의 시작/끝 시간에서, 그 앞에 잘려나간 구간들의 총 길이를 빼야 함
  - 잘린 구간에 걸쳐 있는 자막은 앞뒤로 분리하거나 경계에 맞춰 조정
  - 완성 후 검증: 마지막 자막의 끝 시간 ≤ final.mp4 총 길이
- `glossary.txt`의 고유명사로 자동 치환 (예: 브류 → Vrew)
- 한 줄 최대 20자, 최대 2줄
- 한 자막 최소 노출 1초 보장
- 인코딩은 UTF-8 (캡컷 임포트 시 한글 깨짐 방지)

### Step 6. 검수 게이트 2 — 자막 확인 ⚠️ 필수
- srt 경로를 알려주고 직접 수정할 시간을 줌
- "수정 다 했으면 엔터" 방식으로 대기
- 수정된 파일을 다시 읽어서 최종 출력에 반영

### Step 7. 렌더 + 검증
- FFmpeg로 **컷만** 적용 → `output/final.mp4` (자막 굽지 않음)
- 재인코딩 최소화 (가능하면 스트림 카피, 안 되면 고화질 재인코딩)
- srt를 `output/final.srt`로 복사
- 렌더 후 자동 검증:
  - 길이가 예상값과 ±2초 이내인가
  - 해상도가 원본과 같은가
  - 오디오 트랙이 살아있는가
  - **srt 마지막 타임코드가 영상 길이를 넘지 않는가** (밀림 감지)
- 하나라도 실패하면 원인과 함께 알림

---

## 4. 캡컷에서 이어서 하는 작업 (도구 범위 밖)

도구가 끝나면 사용자가 캡컷에서 직접:
1. final.mp4 임포트
2. final.srt 임포트 → 자막 자동 배치
3. 폰트·크기·색상·위치는 캡컷에서 선택
4. B-roll, 효과음, 인트로/아웃트로, 강조 자막 추가

→ **따라서 이 도구는 자막 폰트·스타일을 일절 다루지 않는다.**

---

## 5. 크로스플랫폼 대응

- 경로는 전부 `pathlib` 사용 (Windows 역슬래시 문제 방지)
- FFmpeg / Whisper 실행 파일 경로를 config.yaml로 분리
- 최초 실행 시 ffmpeg, whisper 설치 여부 자동 확인 후 설치 안내

---

## 6. 실패 대비 (하나 실패해도 전체 중단 금지)

| 실패 지점 | 대체 동작 |
|---|---|
| Whisper 전사 실패 | 기존 transcript.json 있으면 재사용 |
| 오디오 트랙 없음 | 해당 파일만 제외하고 진행 |
| 촬영 시각 메타데이터 없음 | 파일 수정일로 폴백 + 경고 표시 |
| 클립별 해상도/fps 불일치 | 경고 후 사용자에게 통일/중단 선택 요청 |
| 병합 실패 | work/sources.json 남겨서 재시작 가능 |
| FFmpeg 렌더 실패 | 설치 상태 점검 후 안내 |
| 컷 렌더 실패 | srt만이라도 출력 (원본 영상 그대로 쓸 수 있게) |
| 타임코드 검증 실패 | 렌더 중단하고 어긋난 자막 번호 표시 |

중간 산출물은 항상 `work/`에 남겨서 실패 지점부터 재시작 가능하게.

---

## 7. 이번 버전에서 제외 (나중에)

- 자막 하드코딩 / 폰트 스타일링 → 캡컷에서 처리
- 하이라이트 자동 추출 (쇼츠 후보)
- 9:16 / 1:1 비율 자동 변환
- B-roll 자동 삽입
- 캡컷 프로젝트 파일 직접 생성

---

## 8. Claude Code에 요청할 순서 + 모델 선택

세션 중 `/model` 명령어로 모델을 전환한다.
Opus는 토큰 소모가 크므로 **설계가 어려운 구간에만** 쓰고, 나머지는 Sonnet으로 내린다.

| 단계 | 모델 | 작업 |
|---|---|---|
| 1 | **Opus** | 기획서 전달 → 전체 구조 설계, 파일 구성, 확인 질문 받기 |
| 2 | Sonnet | Step 0~1.5 구현 (파일 수집·메타데이터 정렬·필터·검수 게이트) |
| 3 | Sonnet | Step 1.8~2 구현 (병합 + Whisper 전사) |
| 4 | Sonnet | Step 3~4 구현 (무음·필러워드 컷 리스트) |
| 5 | **Opus** | **Step 5 타임코드 재계산 로직 설계** ← 이 프로젝트에서 가장 어려운 부분 |
| 6 | Sonnet | Step 5 나머지 구현 (srt 포맷 출력, 용어집 치환, 줄바꿈) |
| 7 | Sonnet | Step 6~7 구현 (검수 게이트, FFmpeg 렌더, 검증) |
| 8 | Sonnet | 버그 수정, 실제 촬영분 테스트, 반복 작업 |

**Opus를 쓰는 이유 (5단계)**
컷 구간의 누적 오프셋을 빼서 자막 시간을 앞당기고, 컷 경계에 걸친 자막을 분할하는 로직.
한 번 잘못 짜면 짧은 영상에선 티가 안 나다가 긴 영상에서 미묘하게 어긋난다.
→ **설계만 Opus로 받고, 실제 코드 작성부터는 Sonnet으로 전환할 것.**

**Sonnet으로 충분한 이유**
ffprobe 메타데이터 추출, FFmpeg 명령어 조립, concat 처리, srt 포맷 출력, 파일 정렬은
전부 정형화된 작업이라 Sonnet이 빠르고 저렴하게 처리한다.

---

### 진행 순서 상세

1. **[Opus]** "이 기획서 읽고, 만들기 전에 확인할 게 있으면 먼저 물어봐"
2. **[Sonnet]** Step 0~1.5만 먼저 만들어서 테스트 (파일이 제대로 걸러지고 순서가 맞는지)
3. **[Sonnet]** Step 1.8~2 추가 (병합 + 전사가 잘 되는지)
4. **[Sonnet]** Step 3~4 추가 (컷 리스트가 말이 되는지 짧은 클립 2~3개로 확인)
5. **[Opus]** Step 5 타임코드 재계산 로직 설계 → **[Sonnet]** 구현
6. **[Sonnet]** 캡컷에 임포트해서 **타임코드 밀림 여부 직접 확인**
7. **[Sonnet]** Step 6~7 추가
8. **[Sonnet]** 전체 통합 후 실제 촬영분으로 테스트

한 번에 다 만들지 말고 위 순서대로 나눠서 요청할 것.

---

## 9. 코딩 원칙 (Claude Code에 반드시 지키게 할 것)

이 도구는 개인용이다. 확장성·추상화·프레임워크는 필요 없다.
아래 원칙을 지켜서 **읽고 고칠 수 있는 최소한의 코드**로 만들 것.

### 사다리 — 코드를 쓰기 전에 위에서부터 확인

1. 이게 존재할 필요가 있나?     → 아니면 만들지 않는다 (YAGNI)
2. 이미 이 코드베이스에 있나?   → 다시 쓰지 말고 재사용한다
3. 표준 라이브러리로 되나?      → 쓴다
4. 플랫폼 기본 기능으로 되나?   → 쓴다
5. 이미 설치된 의존성으로 되나? → 쓴다
6. 한 줄로 되나?                → 한 줄로 쓴다
7. 그 다음에야: 동작하는 최소한

이 사다리는 문제를 이해한 *다음에* 적용한다. 문제 파악 대신이 아니다.
변경이 닿는 코드를 읽고 실제 흐름을 추적한 뒤에 단계를 고른다.
해법에는 게으르되, 읽는 데는 절대 게으르지 않는다.

### 게으른 것과 부실한 것은 다르다

신뢰 경계 검증, 데이터 손실 처리, 보안, 접근성은 절대 잘라내지 않는다.

이 프로젝트에서 구체적으로:
- **원본 파일 보호** — 읽기 전용 접근, 수정·이동·삭제 금지. 이건 타협 없음.
- **타임코드 검증** — 자막이 영상 길이를 넘는지 반드시 확인
- **덮어쓰기 방지** — output/에 같은 이름 파일이 있으면 확인 후 진행
- **에러를 조용히 삼키지 않기** — 파일이 제외되면 이유를 반드시 표시

목표는 "토큰 최소"가 아니라 **필요한 것만 쓰기**다.
결과적으로 코드가 짧아지는 것이지, 짧게 만드는 게 목적이 아니다.

### 이 프로젝트에서 하지 말 것

- 클래스 계층·플러그인 구조·의존성 주입 (파일 몇 개면 충분)
- 설정 프레임워크 (yaml 하나 읽으면 끝)
- 자체 로깅 시스템 (print로 충분)
- FFmpeg 래퍼 라이브러리 설치 (subprocess로 명령어 직접 호출)
- 쓰이지 않을 예외 클래스, 인터페이스, 추상 베이스

### 잘 되고 있다는 신호

- diff에 불필요한 변경이 적다
- 과설계 때문에 다시 갈아엎는 일이 적다
- 실수한 뒤가 아니라 **구현 전에** 확인 질문이 나온다

---

## 10. (선택) ponytail 플러그인

위 원칙을 매번 말로 설명하는 대신, 이걸 자동으로 주입해주는 Claude Code 플러그인이 있다.

```
/plugin marketplace add DietrichGebert/ponytail
/plugin install ponytail@ponytail
```

(설치는 두 번의 프롬프트로 나눠 보내야 동작한다)

- `/ponytail-review` — 지금 diff에서 지울 것을 찾아준다
- `/ponytail lite | full | ultra | off` — 강도 조절
- Node.js가 PATH에 있어야 라이프사이클 훅이 동작한다

MIT 라이선스. 설치는 선택 사항이고, 9번 원칙을 직접 지키게 해도 결과는 같다.

> 이 리포에서는 `.claude/settings.json`에 이미 등록되어 있다 (`extraKnownMarketplaces`).
